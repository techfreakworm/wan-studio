# Wan Studio — pinned dependencies
# Target: HF ZeroGPU (Blackwell sm_120) + local MPS dev (M5 Max)
# Refer to RESEARCH.md §12 for the full dependency matrix and rationale.

# Core ML
torch>=2.8.0,<2.11          # sm_120 Blackwell support
diffusers>=0.38.0           # load_into_transformer_2 for Wan 2.2 MoE LoRA (PR #12074)
transformers>=4.45          # UMT5EncoderModel, CLIPVisionModel
accelerate>=0.34
peft>=0.13

# HF infrastructure
huggingface_hub>=0.27       # snapshot_download, Storage Bucket
spaces>=0.50.2              # ZeroGPU @spaces.GPU + AOTI API
gradio>=5.4                 # gr.Sidebar, gr.Video buttons, route()

# Quantization (ZeroGPU large only; guarded by backend check)
torchao>=0.7

# Video / image I/O
opencv-python-headless>=4.10
imageio>=2.36
imageio-ffmpeg>=0.5
Pillow>=11.0
numpy>=2.0
einops>=0.8

# Audio (S2V mode)
librosa>=0.10
soundfile>=0.13

# Dev / utils
python-dotenv>=1.0
